'use strict';

// example script to test
// the test command tests if rules are valid and if they execute without any internal error

/**
 * Bar
 * @returns {number} number
 */
function bar() {
  return 1;
}

bar();
